<h1>Listing group_details</h1>

<table>
  <tr>
  </tr>

<% @group_details.each do |group_detail| %>
  <tr>
    <td><%= link_to 'Show', group_detail %></td>
    <td><%= link_to 'Edit', edit_group_detail_path(group_detail) %></td>
    <td><%= link_to 'Destroy', group_detail, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New group_detail', new_group_detail_path %>